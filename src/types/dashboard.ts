
export interface UserData {
  firstName: string;
  lastName: string;
  businessName: string;
  email?: string;
  phoneNumber?: string;
  role?: string;
  // We could add more user fields here as needed
}
